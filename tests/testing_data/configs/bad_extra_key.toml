root_dir = '$CSTOW_TESTING_DATA'
cmd_template = 'echo "action: $action\ntarget: $target\ndir: $dir\n" && stow --$action --no-folding --verbose -d $dir -t $target .'
extra_key = 'asdf'

[targets_dirs]
'$CSTOW_TESTING_DATA/target' = ['dir']
